# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=gcr.io/varsha-project/gcrpreci18feb-ghcustomnode .

  pre_ci_boot:
    image_name: drydock/u12nod #gcr.io/varsha-project/gcrpreci17feb
    image_tag: prod
    pull: true
    #options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=gcr.io/varsha-project/gcrpreci18feb-ghcustomnode:new .
    - docker push gcr.io/varsha-project/gcrpreci18feb-ghcustomnode:latest

integrations:
  hub:
    - integrationName: ship-gcr
      type: gcr  
